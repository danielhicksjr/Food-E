<%= @post.image %>
<%= @post.title %>
<%= @post.body %>


<% if @post.user == current_user %>
    <%= link_to “Delete Post”, post_path(@.id), {method: ‘delete’, data: {confirm: “Are you sure?“}} %>  
<% end %>
