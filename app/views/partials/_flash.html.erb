<!-- Create a partial for _flash -->
<% flash.each do |key, value| %>
	<div class="alert alert-<%= key %>">
		<%= value %>
	</div>
<% end %>

<!-- Add the partial to your application.html.erb -->

<!-- Use flash messages -->
